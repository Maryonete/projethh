<form
  method="post"
  action="{{ path('president_delete', { id: president.id }) }}"
  onsubmit="return confirm('Etes vous sur de vouloir supprimer ce président?');"
>
  <input
    type="hidden"
    name="_token"
    value="{{ csrf_token('delete' ~ president.id) }}"
  />
  <button class="btn btn-primary">Supression</button>
</form>
